
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartrt.app',
  appName: 'Smart RT',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
